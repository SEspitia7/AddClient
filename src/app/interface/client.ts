/* Defining the structure of the object that will be returned by the API. */
export interface Client {
    id:Number,
    doc_type:String,
    doc_number:String,
    name:String,
    last_name:String,
    email:String,
    phone_number:Number
}
